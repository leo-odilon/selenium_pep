name: Main Workflow

on:
  push:
    branches:
      - master
  # schedule:
  #   - cron: '0 0 * * *'  # Executa todos os dias à meia-noite

jobs:
  download-pep:
    uses: ./.github/workflows/download_pep.yml

  download-csnu:
    uses: ./.github/workflows/download_csnu.yml
