name: Main Workflow

on:
  push:
    branches:
      - master
  # schedule:
  #   - cron: '0 0 * * *'  # Executa todos os dias à meia-noite

jobs:
  download:
    uses: ./.github/workflows/download.yml

  # download-csnu:
  #   uses: ./.github/workflows/download_csnu.yml

  # open-file:
  #   needs: [download]
  #   uses: ./.github/workflows/open_file.yml

  # open-csnu:
  #   needs: [download-csnu]
  #   uses: ./.github/workflows/open_csnu.yml

  prepare_data:
    needs: [download]
    uses: ./.github/workflows/prepare_data.yml

  insert_data:
    needs: [prepare_data]
    uses: ./.github/workflows/insert_data.yml
